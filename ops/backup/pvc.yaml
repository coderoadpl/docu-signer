apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: agentproofarch-backups
  namespace: agentproofarch-backup
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 20Gi
